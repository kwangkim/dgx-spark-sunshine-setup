# /etc/default/tailscale-autoconnect
# Optional extra args for `tailscale up`.
#
# Recommended default: leave empty.
# Example (enable Tailscale SSH):
#   TS_UP_EXTRA_ARGS="--ssh"
TS_UP_EXTRA_ARGS=""
